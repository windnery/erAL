# M0 存档读档 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现完整的存档/读档能力（后端序列化 + 前端接线），并建立项目测试设施。

**Architecture:** 新增 `SaveManager` 持有 world 引用，采用"存 delta 不存全量"协议：静态数据（lines/schedule/talent 初始值）读档时从 `data/` JSON 重建，存档只存运行时字段（player 9 项、shipgirl 9 项、time/work/menu_active/secretary_ship_id）。序列化/反序列化为纯函数（可单测），槽位管理（3 槽）负责落盘。前端主菜单"继续经营"与游戏内 Ex_COM 的 save/load 按钮接线，移除 CommandManager 的硬编码补丁，改用注册表真指令。

**Tech Stack:** Python 3.11+ 标准库（json/pathlib/datetime/unittest），pywebview，前端原生 JS。

---

## 文件结构

| 文件 | 责任 |
|---|---|
| `game_engine/managers/SaveManager.py`（新建） | 序列化协议 + 槽位管理 |
| `tests/__init__.py`、`tests/test_save_manager.py`（新建） | 单测设施 |
| `game_engine/commands/system.py`（新建） | save/load 注册指令 |
| `game_engine/managers/CommandManager.py`（修改） | 移除硬编码补丁；`get_cmd_options` 加 save/load 槽位 |
| `world.py`（修改） | World 构造 `save_manager` |
| `api.py`（修改） | 挂载 `save_manager` 到 managers |
| `frontend/js/api.js`（修改） | 新增 getSaveList/doSave/doLoad |
| `frontend/js/main.js`（修改） | `load_game()` 实现（继续经营） |
| `.gitignore`（修改） | 加入 `sav/` |
| `data/characters/*.json`（修改） | 为缺失口上的指令补 1 条占位台词（顺手修复） |

依赖链：SaveManager → world（TYPE_CHECKING 引用，避免循环导入）。

---

### Task 1: 测试设施 + serialize_world

**Files:**
- Create: `tests/__init__.py`
- Create: `tests/test_save_manager.py`
- Create: `game_engine/managers/SaveManager.py`

- [ ] **Step 1: 创建测试目录与空测试文件**

```powershell
New-Item -ItemType Directory -Path tests
New-Item -ItemType File -Path tests/__init__.py
```

- [ ] **Step 2: 写失败的序列化测试**

`tests/test_save_manager.py`：

```python
import unittest

from world import World
from game_engine.managers.SaveManager import SaveManager, SAVE_VERSION


class TestSerializeWorld(unittest.TestCase):
    def setUp(self):
        self.world = World()
        self.sm = SaveManager(self.world)

    def test_serialize_contains_runtime_fields(self):
        # 修改运行时字段
        self.world.player.money = 777
        self.world.player.base['stamina'] = 1234
        self.world.time_manager.day = 42
        self.world.time_manager.hour = 15
        self.world.time_manager.minute = 30
        self.world.work_manager.works = 500
        self.world.work_manager.works_done = 300
        self.world.menu_active = False
        laffey = self.world.npc_manager.shipgirls['laffey']
        laffey.favor = 999
        laffey.cflag['secretary_ship'] = True

        data = self.sm.serialize_world()

        self.assertEqual(data['version'], SAVE_VERSION)
        d = data['data']
        self.assertEqual(d['player']['money'], 777)
        self.assertEqual(d['player']['base']['stamina'], 1234)
        self.assertEqual(d['time'], {'day': 42, 'hour': 15, 'minute': 30})
        self.assertEqual(d['work'], {'works': 500, 'works_done': 300})
        self.assertFalse(d['menu_active'])
        self.assertEqual(d['secretary_ship_id'], 'laffey')
        self.assertEqual(d['shipgirls']['laffey']['favor'], 999)
        self.assertTrue(d['shipgirls']['laffey']['cflag']['secretary_ship'])

    def test_serialize_does_not_store_static_data(self):
        data = self.sm.serialize_world()
        sg = data['data']['shipgirls']['laffey']
        self.assertNotIn('lines', sg)
        self.assertNotIn('schedule', sg)
        self.assertNotIn('talent', sg)


if __name__ == '__main__':
    unittest.main()
```

- [ ] **Step 3: 跑测试确认失败**

Run: `python -m unittest discover -s tests -t . -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'game_engine.managers.SaveManager'`

- [ ] **Step 4: 实现 SaveManager（序列化部分）**

`game_engine/managers/SaveManager.py`：

```python
import json
from datetime import datetime
from pathlib import Path
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from world import World

SAVE_VERSION = 1
SLOT_COUNT = 3


class SaveManager:
    """世界状态存档管理：存 delta（仅运行时字段），静态数据读档时从 data/ 重建"""

    def __init__(self, world: 'World', sav_dir: Path | None = None):
        self.world = world
        self.sav_dir = sav_dir or Path('sav')

    def serialize_world(self) -> dict:
        world = self.world
        player = world.player
        sg_state = {}
        for sg in world.npc_manager.get_all_npcs():
            sg_state[sg.id] = {
                'location': sg.location,
                'base': sg.base,
                'favor': sg.favor,
                'trust': sg.trust,
                'abl': sg.abl,
                'exp': sg.exp,
                'juel': sg.juel,
                'palam': sg.palam,
                'cflag': sg.cflag,
            }
        return {
            'version': SAVE_VERSION,
            'meta': {
                'day': world.time_manager.day,
                'hour': world.time_manager.hour,
                'minute': world.time_manager.minute,
                'player_name': player.name,
            },
            'data': {
                'time': {
                    'day': world.time_manager.day,
                    'hour': world.time_manager.hour,
                    'minute': world.time_manager.minute,
                },
                'player': {
                    'location': player.location,
                    'base': player.base,
                    'money': player.money,
                    'wake_time': player.wake_time,
                    'abl': player.abl,
                    'exp': player.exp,
                    'juel': player.juel,
                    'palam': player.palam,
                    'talent': player.talent,
                },
                'shipgirls': sg_state,
                'work': {
                    'works': world.work_manager.works,
                    'works_done': world.work_manager.works_done,
                },
                'menu_active': world.menu_active,
                'secretary_ship_id': (
                    world.npc_manager.secretary_ship.id
                    if world.npc_manager.secretary_ship else None
                ),
            },
        }
```

- [ ] **Step 5: 跑测试确认通过**

Run: `python -m unittest discover -s tests -t . -v`
Expected: PASS — 2 tests ok

- [ ] **Step 6: Commit**

```powershell
git add tests/ game_engine/managers/SaveManager.py
git commit -m "test: 建立 unittest 设施并实现世界序列化"
```

---

### Task 2: deserialize_world（反序列化）

**Files:**
- Modify: `tests/test_save_manager.py`
- Modify: `game_engine/managers/SaveManager.py`

- [ ] **Step 1: 写失败的往返测试**

追加到 `tests/test_save_manager.py`：

```python
class TestDeserializeWorld(unittest.TestCase):
    def setUp(self):
        self.world = World()
        self.sm = SaveManager(self.world)

    def _mutate_world(self, world):
        world.player.money = 555
        world.player.location = {'region': 'office', 'node': 'desk'}
        world.player.wake_time = {'hour': 8, 'minute': 0}
        world.player.base['energy'] = 888
        world.player.abl['talk_abl'] = 4
        world.time_manager.day = 10
        world.time_manager.hour = 20
        world.time_manager.minute = 5
        world.work_manager.works = 111
        world.work_manager.works_done = 99
        world.menu_active = False
        laffey = world.npc_manager.shipgirls['laffey']
        laffey.favor = 2345
        laffey.trust = 678
        laffey.location = {'region': 'home', 'node': 'bedroom'}
        laffey.cflag['sleeping'] = True
        world.npc_manager.set_secretary_ship_proc('z23', world.player)

    def test_roundtrip_restores_state(self):
        self._mutate_world(self.world)
        data = self.sm.serialize_world()

        new_world = World()
        err = SaveManager(new_world).deserialize_world(data)

        self.assertIsNone(err)
        p = new_world.player
        self.assertEqual(p.money, 555)
        self.assertEqual(p.location, {'region': 'office', 'node': 'desk'})
        self.assertEqual(p.wake_time, {'hour': 8, 'minute': 0})
        self.assertEqual(p.base['energy'], 888)
        self.assertEqual(p.abl['talk_abl'], 4)
        self.assertEqual(new_world.time_manager.day, 10)
        self.assertEqual(new_world.time_manager.hour, 20)
        self.assertEqual(new_world.time_manager.minute, 5)
        self.assertEqual(new_world.work_manager.works, 111)
        self.assertEqual(new_world.work_manager.works_done, 99)
        self.assertFalse(new_world.menu_active)
        laffey = new_world.npc_manager.shipgirls['laffey']
        self.assertEqual(laffey.favor, 2345)
        self.assertEqual(laffey.trust, 678)
        self.assertEqual(laffey.cflag['sleeping'], True)
        self.assertEqual(laffey.palam_lv['c_pleasure_palam'], 0)  # 派生值被重算
        self.assertEqual(new_world.npc_manager.secretary_ship.id, 'z23')

    def test_deserialize_corrupt_data_returns_error(self):
        err = self.sm.deserialize_world({'data': {'time': {}}})
        self.assertIsNotNone(err)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m unittest discover -s tests -t . -v`
Expected: FAIL — `AttributeError: 'SaveManager' object has no attribute 'deserialize_world'`

- [ ] **Step 3: 实现 deserialize_world**

追加到 `game_engine/managers/SaveManager.py`：

```python
    def deserialize_world(self, data: dict) -> str | None:
        """把存档数据还原到 world。成功返回 None，失败返回错误信息"""
        try:
            d = data['data']
            tm = d['time']
            self.world.time_manager.day = tm['day']
            self.world.time_manager.hour = tm['hour']
            self.world.time_manager.minute = tm['minute']

            p = d['player']
            self.world.player.location = p['location']
            self.world.player.base = p['base']
            self.world.player.money = p['money']
            self.world.player.wake_time = p['wake_time']
            self.world.player.abl = p['abl']
            self.world.player.exp = p['exp']
            self.world.player.juel = p['juel']
            self.world.player.palam = p['palam']
            self.world.player.talent = p['talent']

            from game_engine.models.shipgirl import ShipGirl
            for sg_id, st in d['shipgirls'].items():
                sg = ShipGirl(**self.world.npc_manager.shipgirls_db[sg_id])
                sg.location = st['location']
                sg.base = st['base']
                sg.favor = st['favor']
                sg.trust = st['trust']
                sg.abl = st['abl']
                sg.exp = st['exp']
                sg.juel = st['juel']
                sg.palam = st['palam']
                sg.cflag = st['cflag']
                sg.update_palam_level()
                self.world.npc_manager.shipgirls[sg_id] = sg

            self.world.work_manager.works = d['work']['works']
            self.world.work_manager.works_done = d['work']['works_done']
            self.world.menu_active = d['menu_active']

            sec_id = d['secretary_ship_id']
            self.world.npc_manager.secretary_ship = (
                self.world.npc_manager.shipgirls[sec_id] if sec_id else None
            )
            self.world.player.update_palam_level()
            return None
        except (KeyError, TypeError, ValueError) as e:
            return f'存档数据损坏：{e}'
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m unittest discover -s tests -t . -v`
Expected: PASS — 5 tests ok

- [ ] **Step 5: Commit**

```powershell
git add tests/ game_engine/managers/SaveManager.py
git commit -m "feat: 实现世界反序列化（delta 协议）"
```

---

### Task 3: 槽位管理（save_game/load_game/get_save_list）

**Files:**
- Modify: `tests/test_save_manager.py`
- Modify: `game_engine/managers/SaveManager.py`

- [ ] **Step 1: 写失败的槽位测试**

追加到 `tests/test_save_manager.py`：

```python
import json
import tempfile
from pathlib import Path


class TestSlots(unittest.TestCase):
    def setUp(self):
        self.tmp = tempfile.TemporaryDirectory()
        self.world = World()
        self.sm = SaveManager(self.world, sav_dir=Path(self.tmp.name))

    def tearDown(self):
        self.tmp.cleanup()

    def test_save_then_load_roundtrip(self):
        self.world.player.money = 4321
        self.world.time_manager.day = 7
        meta = self.sm.save_game(1)
        self.assertEqual(meta['day'], 7)
        self.assertTrue((Path(self.tmp.name) / 'slot_1.json').exists())

        new_world = World()
        err = SaveManager(new_world, sav_dir=Path(self.tmp.name)).load_game(1)
        self.assertIsNone(err)
        self.assertEqual(new_world.player.money, 4321)
        self.assertEqual(new_world.time_manager.day, 7)

    def test_get_save_list_marks_empty_slots(self):
        self.sm.save_game(2)
        slots = self.sm.get_save_list()
        self.assertEqual(len(slots), 3)
        self.assertFalse(slots[0]['has_save'])
        self.assertTrue(slots[1]['has_save'])
        self.assertEqual(slots[1]['slot'], 2)
        self.assertFalse(slots[2]['has_save'])

    def test_load_missing_slot_returns_error(self):
        err = self.sm.load_game(3)
        self.assertEqual(err, '该槽位没有存档')

    def test_load_corrupt_file_returns_error(self):
        (Path(self.tmp.name) / 'slot_1.json').write_text('{broken', encoding='utf-8')
        err = self.sm.load_game(1)
        self.assertIsNotNone(err)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m unittest discover -s tests -t . -v`
Expected: FAIL — `AttributeError: 'SaveManager' object has no attribute 'save_game'`

- [ ] **Step 3: 实现槽位管理**

追加到 `game_engine/managers/SaveManager.py`：

```python
    def _slot_path(self, slot: int) -> Path:
        return self.sav_dir / f'slot_{slot}.json'

    def save_game(self, slot: int) -> dict:
        """保存到槽位，返回 meta（供前端提示）"""
        path = self._slot_path(slot)
        path.parent.mkdir(parents=True, exist_ok=True)
        data = self.serialize_world()
        data['meta']['saved_at'] = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
        with open(path, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=2)
        return data['meta']

    def load_game(self, slot: int) -> str | None:
        """从槽位读档并还原 world。成功返回 None，失败返回错误信息"""
        path = self._slot_path(slot)
        if not path.exists():
            return '该槽位没有存档'
        try:
            with open(path, 'r', encoding='utf-8') as f:
                data = json.load(f)
        except (json.JSONDecodeError, OSError) as e:
            return f'存档读取失败：{e}'
        return self.deserialize_world(data)

    def get_save_list(self) -> list[dict]:
        """返回 3 个槽位信息：{slot, has_save, day, hour, minute, player_name}"""
        result = []
        for slot in range(1, SLOT_COUNT + 1):
            entry = {'slot': slot, 'has_save': False}
            path = self._slot_path(slot)
            if path.exists():
                try:
                    with open(path, 'r', encoding='utf-8') as f:
                        meta = json.load(f).get('meta', {})
                    entry.update({
                        'has_save': True,
                        'day': meta.get('day'),
                        'hour': meta.get('hour'),
                        'minute': meta.get('minute'),
                        'player_name': meta.get('player_name', ''),
                    })
                except (json.JSONDecodeError, OSError):
                    entry['has_save'] = False
            result.append(entry)
        return result
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m unittest discover -s tests -t . -v`
Expected: PASS — 9 tests ok

- [ ] **Step 5: Commit**

```powershell
git add tests/ game_engine/managers/SaveManager.py
git commit -m "feat: 存档槽位管理（save/load/列表）"
```

---

### Task 4: 后端接线（save/load 指令 + world + api）

**Files:**
- Create: `game_engine/commands/system.py`
- Modify: `game_engine/commands/__init__.py`
- Modify: `game_engine/managers/CommandManager.py`
- Modify: `world.py`
- Modify: `api.py`

- [ ] **Step 1: 注册 save/load 指令**

`game_engine/commands/system.py`：

```python
from __future__ import annotations
from typing import TYPE_CHECKING

from game_engine.commands._commands import register_cmd

if TYPE_CHECKING:
    from world import World


@register_cmd('save', '存档', '系统', needs_target=False)
def save(world: World, option=None):
    """存档：option 为槽位 key（'1'/'2'/'3'）"""
    if option is None:
        return ['请选择存档槽位']
    slot = int(option)
    meta = world.save_manager.save_game(slot)
    return [f'已保存到槽位{slot}（第{meta["day"]}天 {meta["hour"]}:{str(meta["minute"]).zfill(2)}）']


@register_cmd('load', '读档', '系统', needs_target=False)
def load(world: World, option=None):
    """读档：option 为槽位 key（'1'/'2'/'3'）"""
    if option is None:
        return ['请选择存档槽位']
    slot = int(option)
    err = world.save_manager.load_game(slot)
    if err:
        return [err]
    return [f'读取了槽位{slot}的存档']
```

`game_engine/commands/__init__.py` 追加：

```python
from . import movement, daily, menu, system
```

- [ ] **Step 2: CommandManager 移除硬编码 + 槽位选项**

`game_engine/managers/CommandManager.py` 的 `_get_system_commands` 删除：

```python
        # save/load 未走注册表，先硬编码补上（保持原功能）
        sys_com += [{'key': 'save', 'name': '存档', 'cat': '系统'},
                    {'key': 'load', 'name': '读档', 'cat': '系统'}]
```

在 `get_cmd_options` 的 `elif command == 'set_secretary_ship':` 分支后追加：

```python
        elif command in ('save', 'load'):
            slots = self.world.save_manager.get_save_list()
            return [
                {
                    'key': str(s['slot']),
                    'name': (f'槽位{s["slot"]}：第{s["day"]}天 '
                             f'{s["hour"]}:{str(s["minute"]).zfill(2)} {s["player_name"]}')
                    if s['has_save'] else f'槽位{s["slot"]}：空',
                }
                for s in slots
            ]
```

- [ ] **Step 3: world.py 挂载 save_manager**

`world.py` 顶部 import 追加 `from game_engine.managers.SaveManager import SaveManager`，`__init__` 中 `self.menu_active = True` 之后追加：

```python
        self.save_manager = SaveManager(self)
```

- [ ] **Step 4: api.py 挂载 save_manager**

`api.py` 修改：

```python
from world import World
from game_engine.managers.SaveManager import SaveManager


class Api:
    def __init__(self):
        world = World()
        self.managers = {
            'world': world,
            'map_manager': world.map_manager,
            'command_manager': world.command_manager,
            'time_manager': world.time_manager,
            'save_manager': world.save_manager,
        }
```

- [ ] **Step 5: 冒烟验证后端指令**

Run: `python -m unittest discover -s tests -t . -v`
Expected: PASS — 9 tests ok（无回归）

Run:

```powershell
python -c "from api import Api; a=Api(); print(a.call('command_manager','get_cmd_options','save')); print(a.call('command_manager','do_cmd','save','1')); print(a.call('world','get_state',None)['time'])"
```

Expected: 输出槽位选项列表、`已保存到槽位1（第1天 ...）`、当前时间

- [ ] **Step 6: Commit**

```powershell
git add game_engine/commands/system.py game_engine/commands/__init__.py game_engine/managers/CommandManager.py world.py api.py
git commit -m "feat: 接入存档指令与 SaveManager"
```

---

### Task 5: 前端接线（api.js + main.js 继续经营）

**Files:**
- Modify: `frontend/js/api.js`
- Modify: `frontend/js/main.js`

- [ ] **Step 1: api.js 新增三个导出**

`frontend/js/api.js` 末尾追加：

```js
export async function getSaveList() {
    return await call('save_manager', 'get_save_list');
}

export async function doSave(slot) {
    return await call('save_manager', 'save_game', slot);
}

export async function doLoad(slot) {
    return await call('save_manager', 'load_game', slot);
}
```

- [ ] **Step 2: main.js 实现 load_game**

`main.js` 顶部 import 行追加：

```js
import { getState, getCmdOptions, doCmd, getSaveList, doLoad } from './api.js';
```

将 `load_game` 空函数替换为：

```js
async function load_game() {
    const slots = await getSaveList();
    const options = slots.map(s => ({
        key: s.slot,
        name: s.has_save
            ? `槽位${s.slot}：第${s.day}天 ${String(s.hour).padStart(2, '0')}:${String(s.minute).padStart(2, '0')} ${s.player_name}`
            : `槽位${s.slot}：空`,
    }));
    showFullscreenOptions(options, async (opt) => {
        const err = await doLoad(opt.key);
        if (err) {
            document.getElementById('fullscreen_options').style.display = 'none';
            showFullscreenText([err]);
            return;
        }
        document.getElementById('fullscreen_options').style.display = 'none';
        document.getElementById('main_menu').style.display = 'none';
        selectedNpcId = null;
        refresh();
    });
}
```

- [ ] **Step 3: 手动验证（游戏内）**

1. `python main.py` 启动，点"初入港区"进入游戏。
2. 移动几步、做一次交互，点 Ex_COM 的"存档"→ 选槽位 1 → 出现"已保存"提示。
3. 关闭游戏重新启动 → 点"继续经营" → 选中槽位 1 → 回到游戏界面。
4. 断言：位置、金钱、时间、选中面板与存档前一致；立绘/头像正常。

- [ ] **Step 4: 手动验证（游戏内读档）**

1. 继续上一步的会话，再改一些状态（移动、消耗体力），点 Ex_COM"读档"→ 槽位 1。
2. 断言：状态回退到存档点，界面正常，无报错。

- [ ] **Step 5: Commit**

```powershell
git add frontend/js/api.js frontend/js/main.js
git commit -m "feat: 前端存档读档接线（主菜单继续经营）"
```

---

### Task 6: .gitignore + 口上补全（顺手修复）

**Files:**
- Modify: `.gitignore`
- Modify: `data/characters/Z23.json`、`data/characters/拉菲.json`、`data/characters/标枪.json`、`data/characters/绫波.json`

- [ ] **Step 1: .gitignore 加入 sav/**

`.gitignore` 末尾追加：

```
sav/
```

- [ ] **Step 2: 盘点缺失口上的指令**

Run:

```powershell
python -c "from world import World; w=World(); acts=['talk','work_together','rub_the_head','body_touch','hug','poke_the_cheek','pinching_cheeks','request_a_lap_pillow','rub_the_belly','rub_the_butt']; [print(sg.id, [a for a in acts if not any(l['action']==a for l in sg.lines)]) for sg in w.npc_manager.get_all_npcs()]"
```

Expected: 输出每只舰娘缺失口上的指令 action 列表（如 `laffey ['work_together', 'body_touch', ...]`）

- [ ] **Step 3: 为每个缺失 action 补 1 条占位台词**

以 `data/characters/拉菲.json` 的 `lines` 数组为例，在每个角色 JSON 的 `lines` 末尾追加（action 名称替换为盘点结果中的缺失项，文案按角色性格写一句通用台词）：

```json
        {
            "action": "rub_the_belly",
            "conditions": {},
            "texts": [
                "{name}摸了摸肚子，歪着头看着你。"
            ]
        },
        {
            "action": "rub_the_butt",
            "conditions": {},
            "texts": [
                "{name}被碰到屁股，红着脸跳开了。"
            ]
        }
```

注意：`talk`/`rub_the_head` 已有台词的角色不要重复添加；JSON 语法用 `python -m json.tool` 校验。

- [ ] **Step 4: 校验 JSON 合法性**

Run:

```powershell
Get-ChildItem data/characters/*.json | ForEach-Object { python -m json.tool $_.FullName | Out-Null; if ($?) { "OK: $($_.Name)" } }
```

Expected: 全部输出 OK

- [ ] **Step 5: Commit**

```powershell
git add .gitignore data/characters/
git commit -m "fix: 补全舰娘缺失口上并忽略 sav 目录"
```

---

### Task 7: 全流程回归验证

**Files:** 无

- [ ] **Step 1: 全量单测**

Run: `python -m unittest discover -s tests -t . -v`
Expected: 9 tests ok

- [ ] **Step 2: 完整手动流程**

1. 新游戏 → 设定起床时间 → 设秘书舰 → 睁开眼睛。
2. 移动到食堂（跨区域），与在场舰娘会话/摸头，确认数值变化。
3. 存档到槽位 1 → 继续玩（移动、工作）→ 读档 → 断言回到存档点。
4. 体力耗尽 → 结算 → 新一天缓冲菜单 → 存档到槽位 2 → 重启 → 继续经营 → 槽位 2。
5. 睡觉 → 日终结算 → 断言 juel/abl 结算正常显示。
6. 检查：存档/读档后 `palam_lv` 进度条、cflag 标记（秘书舰图标）显示正确。

- [ ] **Step 3: 完成 M0，更新 todo.md**

`todo.md` 追加一行：

```
- [x] M0 存档读档（SaveManager + 前端接线 + unittest 设施）
```

Run:

```powershell
git add todo.md
git commit -m "docs: M0 存档读档完成"
```
