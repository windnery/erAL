# 项目高危逻辑审计

> 审计日期：2026-08-23
>
> 审计基线：`feature/new-features` / `5ebd3c2a8535cfb45e8b0b24213747924bf8cf41`
>
> 审计范围：正常或较常见玩家路径下，可能造成存档污染、核心状态互相矛盾、资源/日终重复结算或关键流程中断的逻辑缺陷。极低概率并发、纯样式、纯文案和需要先篡改本地程序的攻击不计入高危结论。

## 1. 结论

当前项目发现 **6 项高危逻辑问题**，未发现符合本次门槛的远程代码执行、任意命令执行或敏感信息泄露问题。

| 编号 | 严重度 | 问题 | 常见可达条件 | 主要后果 |
|---|---|---|---|---|
| HR-01 | HIGH | 失败读档会部分覆盖当前世界 | JSON 合法但字段缺失、迁移不完整或静态角色表变化 | 当前游戏变成两份存档的混合状态 |
| HR-02 | HIGH | 旧版存档继承当前会话的背包/皮肤 | 游戏中读取缺少 `items`/`skins` 的 v1 存档 | 跨档携带或复制道具、皮肤状态 |
| HR-03 | HIGH | 困倦扣到 0 体力不触发昏厥 | 清醒超过 16 小时且剩余体力不足 | 0 体力仍可移动或继续可用指令 |
| HR-04 | HIGH | 耗尽结算嵌套在指令中，结算后指令继续执行 | 工作或调教指令的体力消耗触底 | 同一天被当作新一天结算，结算后又写入新状态 |
| HR-05 | HIGH | 调教中耗尽后旧会话和持续指令不结束 | 低体力执行调教或自动重复持续指令 | 缓冲菜单与调教态同时为真，跨结算继续旧会话 |
| HR-06 | HIGH | 普通计时跨午夜只增加天数，不结算日状态 | 深夜执行任意跨过 24:00 的计时指令 | 工作、PALAM、JUEL 与显示日数分属不同“天” |

## 2. 高危问题

### HR-01：失败读档不是事务性的，会污染当前世界

**代码证据**

- `game_engine/managers/SaveManager.py:98-183` 的 `deserialize_world()` 依次直接覆盖当前 `World` 的时间、玩家、舰娘、工作、秘书舰、皮肤、道具和调教状态。
- 异常只在 `game_engine/managers/SaveManager.py:184-185` 被转换为“存档数据损坏”，此前已经写入的字段没有回滚。
- 前端在 `frontend/js/main.js:288-293` 收到错误后只展示错误文本，不会重建 `World`。

**复现结果**

构造一份前半段有效、后段缺少 `train.leader` 的存档，并把日数和金钱改为 99、777：

```text
加载前: day=1, money=0, train_mode=False
返回值: 存档数据损坏：'leader'
加载后: day=99, money=777, train_mode=False
```

用户看到的是“读档失败”，内存中的世界却已经被部分替换。若继续游戏或覆盖存档，会把混合状态永久写入槽位。

**建议修复方向**

1. 在任何运行时状态写入前完成完整结构、版本、角色 ID、调教参与者和数值边界解析。
2. 将存档先解析成独立的、带类型的快照；全部成功后再一次性替换当前世界。
3. 添加失败不变性测试：任何读档错误后，加载前后的 `serialize_world()` 必须完全一致。

### HR-02：v1 旧档会继承当前会话的背包和皮肤

**代码证据**

- `game_engine/managers/SaveManager.py:155-164` 只有在存档实际包含 `skins`/`items` 时才覆盖对应管理器。
- v1 存档按设计没有这两个字段；在全新 `World` 上加载时测试能够通过，但在游戏中使用系统“读档”时，管理器已经包含当前会话状态。
- `game_engine/commands/system.py:70-79` 允许在当前世界中直接读档，并不会先创建全新世界。

**复现结果**

当前会话先持有 5 个誓约之戒，再读取不含 `items`/`skins` 的 v1 存档：

```text
加载返回: None（成功）
加载前背包: {'oath_ring': 5}
加载后背包: {'oath_ring': 5}
```

旧档本身没有这些道具，但读档成功后继承了当前会话库存。玩家随后保存即可把跨档状态固化。

**建议修复方向**

- 版本迁移必须从“该版本的明确默认值”生成缺失字段，不能保留当前运行时值。
- `items` 应迁移为空库存；`skins` 应从静态数据重建默认拥有/穿戴集合。
- 新增“非全新世界加载 v1”回归测试，分别验证库存、已购皮肤和穿戴皮肤不会跨档泄漏。

### HR-03：困倦消耗绕过统一耗尽结算

**代码证据**

- `game_engine/managers/TimeManager.py:97-109` 统计困倦分钟后直接调用 `player.set_stamina()` 和 `player.set_energy()`。
- `set_stamina()` 在 `game_engine/models/character.py:27-30` 会返回体力是否仍大于 0，但这里忽略了返回值。
- 真正触发昏厥和强制休息的入口是 `world.py:93-100` 的 `World.change_stamina()`，困倦路径没有经过它。
- 移动/离开等系统指令没有体力可用性门槛，见 `game_engine/commands/system.py:11-51`。

**复现结果**

```text
条件: 23:00, stamina=5, energy=100, menu_active=False
推进 10 分钟后: stamina=0, energy=90, menu_active=False
随后: 工作指令仍被列为可用
```

这不是单纯少一条提示：玩家已经进入其他代码认为不应继续的 0 体力状态，却没有发生日终/休息状态转换。

**建议修复方向**

- 所有玩家体力扣减必须收敛到同一个耗尽入口。
- 困倦跨越 0 体力时只触发一次耗尽结果，并立即停止本次时间推进之后的命令效果。
- 增加“困倦前体力小于推进分钟数”的测试，断言恢复、菜单状态和命令中止行为。

### HR-04：耗尽日终发生在指令中途，调用者仍继续修改新状态

**代码证据**

- 大量指令先 `CommandContext.advance_time()`，再计算产出并调用 `CommandContext.consume()`；例如 `game_engine/commands/daily.py:75-102` 的工作指令。
- `game_engine/commands/_context.py:31-48` 调用 `World.change_stamina()`。
- `world.py:93-100` 在体力触底时同步调用完整的 `settle_day(exhaustion=True)`。
- `world.py:161-200` 随后执行工资/罚款、珠转换、能力/素质检查、清空当日状态、生成新工作量并打开缓冲菜单。
- 控制流返回原指令后，原指令仍继续追加经验、好感、PALAM 和输出。

**复现结果**

在第 1 天 7:00、体力 100、剩余工作量 500 时执行工作：

```text
结果时间: 第 1 天 18:00
menu_active: True
工作量: 已被重置为新随机值
```

日数仍是第 1 天，系统却已经执行完整“新的一天”结算；工作处理器在结算后还继续生成经验和结果文本。调教场景中更会在清空当日 PALAM 后重新写入本次指令的 PALAM。

**建议修复方向**

1. 资源扣减返回明确的“继续/耗尽”结果，命令一旦耗尽立即终止后续效果处理。
2. 区分“昏厥休息”和“日终结算”的语义；如果昏厥代表进入下一天，应推进到统一醒来点并保证日数一致。
3. 日终只能由顶层命令编排器执行，避免在任意深层资源函数中重入世界结算。

### HR-05：耗尽后调教会话和持续指令仍然活动

**代码证据**

- `game_engine/managers/CommandManager.py:130-148` 在手动调教指令后继续调用所有活动持续指令。
- 持续指令可在内部通过 `CommandContext.consume()` 触发耗尽结算。
- `world.py:198-200` 只设置 `menu_active=True`，不调用 `TrainManager.end_train()`。
- `game_engine/managers/TrainManager.py:74-77` 才是同时清除 `train` 和 `train_mode` 的唯一正常出口。

**复现结果**

玩家体力 10 时首次执行持续“爱抚”：

```text
menu_active=True
train_mode=True
train is not None
player stamina=2000
continuous_commands=['caress']
目标 PALAM 合计=208（在日终清空后又被本指令写入）
```

缓冲菜单和调教模式本应互斥，却同时为真；重新睁眼后可以沿用结算前的参与者、地点和持续指令。

**建议修复方向**

- 任何睡眠、昏厥或日终结算进入缓冲菜单前，统一结束调教会话并清空持续状态。
- 持续指令循环收到耗尽结果后立即停止，不再执行余下活动指令。
- 测试普通调教耗尽、持续指令自身耗尽、持续指令自动重复耗尽三种路径。

### HR-06：跨午夜只更新日期，不处理日状态边界

**代码证据**

- `game_engine/managers/TimeManager.py:21-30` 在小时达到 24 时只执行 `day += 1`。
- `game_engine/managers/TimeManager.py:86-134` 的统一事件推进会更新困倦和 NPC 位置，但不会通知 `World` 日期已经变化。
- 工作、PALAM/JUEL、能力检查和新工作量只在 `world.py:116-200` 的 `settle_day()` 中处理。

**复现结果**

让普通“爱抚”从第 1 天 23:59 推进到第 2 天 00:04：

```text
日期: day 1 -> day 2
works: 777 -> 777
works_done: 123 -> 123
目标 PALAM: 旧值继续保留，并叠加本次指令结果
```

这条路径不依赖损坏数据或并发，只要玩家深夜继续行动就可到达。若项目规定“日结只在睡觉时发生”，当前日期仍在午夜先递增，因此 UI 日期和工作/PALAM 所属周期依然不一致；代码中也没有独立的“工作日/结算周期”标识来表达这种设计。

**建议修复方向**

- 明确唯一日边界：午夜结算，或睡眠结算，二者只能有一个作为工作/PALAM 周期真相。
- 若选择午夜，时间推进应返回跨日事件，由顶层编排器在命令效果提交前完成一次结算。
- 若选择睡眠，日期显示与日状态应共享显式周期 ID，避免 `day` 已改变但“今日工作”仍属于前一天。
- 新增普通指令和持续指令分别跨午夜的测试，确保每个跨越只结算一次。

## 3. 验证结果

### 五路审查结论

| 审查方向 | 结论 | 说明 |
|---|---|---|
| 目标与约束 | FAIL | 确认困倦、读档原子性和调教耗尽状态为高危 |
| 实际 QA | FAIL | 复现失败读档、跨午夜和训练耗尽残留 |
| 代码质量 | FAIL | 状态边界和中途结算存在阻断项 |
| 安全边界 | PASS | 当前本地架构下未发现符合门槛的 HIGH/CRITICAL 安全利用链 |
| 历史与上下文 | PASS | 新增膝枕地点限制未违反既有约定 |

综合结论：**REVIEW FAILED / REQUEST CHANGES**。失败来自已复现的状态完整性问题，不是风格意见。

### 自动测试

```text
python -m pytest -q
292 passed, 1 failed
```

唯一失败：`tests/test_train_closed_loop.py::TestEjaculationAndOrgasmFormat::test_ejaculation_order_and_colors`，当前射精消息缺少预期颜色标记。它是既存的展示格式问题，不满足本次高危逻辑门槛。

本批新增功能定向测试：

```text
python -m pytest -q tests/test_save_integrity_v3.py tests/test_continuous_commands.py tests/test_drowsiness.py tests/test_movement_events.py tests/test_todo_completion.py
12 passed
```

现有测试覆盖了各功能的正常路径，但没有覆盖上述跨系统状态转换和失败原子性。

### 已核实但未列为高危

- 舰娘移动来源/去向快照与地图节点解析未发现高危问题。
- 膝枕地点白名单中的 region/node 均存在，展示和执行都会重新检查资格。
- `Api.call()` 缺少显式方法白名单、存档槽位后端未限制 1～3、发布入口保持 `debug=True`，都应作为纵深防御改进；但在当前单机本地 `file://` 架构下，需要先取得本地脚本执行或篡改前端，按本次“非极低概率”门槛不列为高危。
- 日志只记录指令标识、槽位、版本和日数，未发现存档主体或敏感信息泄露。

## 4. 修复优先级

1. **P0：读档事务化。** 先完整解析、校验并构建独立快照，再一次性提交；失败必须保证当前世界字节级等价不变。
2. **P0：重构耗尽控制流。** 禁止在深层资源扣减中直接重入日终；用明确结果中止当前指令和持续循环。
3. **P0：统一模式退出。** 睡眠、昏厥、日终进入菜单前结束调教并清理持续状态。
4. **P1：补齐旧档迁移。** 缺失字段由版本默认值生成，禁止继承当前会话。
5. **P1：统一困倦资源入口。** 困倦扣减与普通体力消耗共享同一耗尽规则。
6. **P1：确定唯一日边界。** 让日期、工作、PALAM/JUEL 和菜单周期由同一事件推进。

建议先修复 P0，再补回归测试；这些问题互相影响，不宜只在单个指令里加临时判断。
